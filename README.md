# 🎰 Bingo-simulator-2026
Bingo !!  Let's get more money!!!!!!!!!!
("2026 元宵加碼期間：三星全中 $1,000 | 中 2 碼 $50")

### **🛠️ 專案必要套件**
本專案使用以下 Python 工具開發：

* **Pandas**: 負責**基礎數據處理**與統計。
* **Streamlit**: 負責製作**互動式網頁介面**。


## 🛠️ 如何在您的電腦運行 (本機佈署)


1. **下載程式碼 (Download)**
   * 在此 GitHub 頁面點擊 `app.py` 檔案。
   * 點擊右側的 **「Download raw file」** 按鈕（或是右鍵另存新檔）將檔案存到您的電腦。
   * 同樣下載 `requirements.txt` 並放在同一個資料夾。

2. **安裝必要套件**
   * 開啟終端機 (Terminal) 或 CMD，切換至檔案路徑後執行：
   ```bash
   pip install -r requirements.txt

3. **啟動模擬器**
   streamlit run streamlit_app.py
